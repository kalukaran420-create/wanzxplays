# Railway / Nixpacks Monorepo Deployment Config for PulseCord

[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']

[phases.install]
cmds = [
  'npm install'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'node server/dist/index.js'
